import { createStyles } from "@mantine/core";

const useStyles = createStyles(() => ({
  listItem: {
    height: "40px",
    position: "relative",
    display: "flex",
    listStyleType: "none",
    margin: 0,
    padding: 0,
  },
}));

export default useStyles;
